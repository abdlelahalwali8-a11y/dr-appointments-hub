# دليل نشر تطبيق PWA تلقائياً

## ✅ الميزات المفعّلة

- ✅ تطبيق PWA كامل قابل للتثبيت على الهواتف والأجهزة اللوحية
- ✅ دعم العمل بدون اتصال (Offline Mode)
- ✅ المزامنة التلقائية عند عودة الاتصال
- ✅ إشعارات تثبيت التطبيق التلقائية
- ✅ تحديثات تلقائية للتطبيق
- ✅ أيقونات احترافية للتطبيق
- ✅ استراتيجيات تخزين مؤقت ذكية

## 🚀 النشر التلقائي عبر Vercel

### الخطوة 1: ربط المشروع بـ Vercel

1. اذهب إلى [vercel.com](https://vercel.com) وسجل دخول
2. اضغط "Add New Project"
3. اختر مستودع GitHub الخاص بك
4. سيتم اكتشاف إعدادات المشروع تلقائياً

### الخطوة 2: إعداد متغيرات البيئة في Vercel

في إعدادات المشروع في Vercel، أضف:

```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### الخطوة 3: إعداد GitHub Actions (اختياري)

لتفعيل النشر التلقائي عبر GitHub Actions:

1. اذهب إلى Settings → Secrets and variables → Actions في مستودع GitHub
2. أضف الأسرار التالية:
   - `VERCEL_TOKEN`: احصل عليه من Vercel → Settings → Tokens
   - `VERCEL_ORG_ID`: موجود في .vercel/project.json بعد أول نشر
   - `VERCEL_PROJECT_ID`: موجود في .vercel/project.json بعد أول نشر
   - `VITE_SUPABASE_URL`: رابط Supabase
   - `VITE_SUPABASE_ANON_KEY`: مفتاح Supabase

## 📱 كيفية تثبيت التطبيق

### على Android:
1. افتح التطبيق في Chrome
2. ستظهر رسالة تثبيت تلقائياً
3. اضغط "تثبيت"

### على iOS:
1. افتح التطبيق في Safari
2. اضغط زر المشاركة
3. اختر "Add to Home Screen"

## 🔄 المزامنة التلقائية

التطبيق يزامن البيانات تلقائياً:
- عند فتح التطبيق
- عند عودة الاتصال بالإنترنت
- كل ساعة للتحقق من التحديثات

## 📦 استراتيجيات التخزين المؤقت

### Network First (للبيانات من Supabase)
- يحاول جلب البيانات من الشبكة أولاً
- إذا فشل، يستخدم البيانات المخزنة مؤقتاً
- مدة صلاحية: 5 دقائق

### Cache First (للملفات الثابتة)
- يستخدم النسخة المخزنة مؤقتاً أولاً
- يوفر سرعة تحميل فائقة
- مدة صلاحية: سنة واحدة للخطوط

## 🔔 الإشعارات

التطبيق يدعم:
- إشعارات التثبيت
- إشعارات التحديثات
- إشعارات حالة الاتصال

## 🛠️ الاختبار المحلي

```bash
npm run build
npm run preview
```

ثم افتح التطبيق في المتصفح وجرب:
- تثبيت التطبيق
- فصل الاتصال بالإنترنت وتجربة الميزات
- إعادة الاتصال ومراقبة المزامنة

## 📊 مراقبة الأداء

يمكنك مراقبة أداء PWA في:
- Chrome DevTools → Application → Service Workers
- Chrome DevTools → Application → Cache Storage
- Lighthouse لتقييم PWA

## 🎯 نصائح للأداء الأفضل

1. التطبيق يعمل بدون اتصال للصفحات المزارة سابقاً
2. الأيقونات والملفات الثابتة مخزنة للوصول السريع
3. البيانات تُحدّث تلقائياً في الخلفية
4. التحديثات الجديدة تظهر فوراً بعد الموافقة

## 🔐 الأمان

- جميع الاتصالات عبر HTTPS
- البيانات الحساسة لا تُخزن محلياً
- Service Worker محدث تلقائياً
- مفاتيح API محمية في متغيرات البيئة

## 📱 التوافق

التطبيق يعمل على:
- ✅ Chrome/Edge (Android & Desktop)
- ✅ Safari (iOS & macOS)
- ✅ Firefox (Android & Desktop)
- ✅ Samsung Internet

## 🆘 استكشاف الأخطاء

### التطبيق لا يظهر رسالة التثبيت؟
- تأكد من فتح التطبيق عبر HTTPS
- تأكد من عدم تثبيت التطبيق مسبقاً
- امسح localStorage وأعد تحميل الصفحة

### البيانات لا تتزامن؟
- تحقق من اتصال الإنترنت
- افحص Console للأخطاء
- تأكد من صحة مفاتيح Supabase

### Service Worker لا يعمل؟
- تأكد من بناء المشروع (npm run build)
- افحص Application → Service Workers في DevTools
- جرب Unregister ثم إعادة التحميل
