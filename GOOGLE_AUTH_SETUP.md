# دليل تفعيل تسجيل الدخول عبر Google

## ✅ تم بالفعل
تم إضافة الكود اللازم لتسجيل الدخول عبر Google في التطبيق. الآن تحتاج فقط إلى إعداد Supabase.

## 📋 خطوات التفعيل

### 1. إعداد Google Cloud Console

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. قم بإنشاء مشروع جديد أو اختر مشروعاً موجوداً
3. اذهب إلى **APIs & Services** > **Credentials**
4. انقر على **CREATE CREDENTIALS** > **OAuth client ID**
5. اختر **Web application**
6. في **Authorized JavaScript origins** أضف:
   ```
   https://hrplussmxgohdvltmaqa.supabase.co
   ```
7. في **Authorized redirect URIs** أضف:
   ```
   https://hrplussmxgohdvltmaqa.supabase.co/auth/v1/callback
   ```
8. احفظ **Client ID** و **Client Secret**

### 2. إعداد Supabase

1. اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard/project/hrplussmxgohdvltmaqa)
2. اذهب إلى **Authentication** > **Providers**
3. ابحث عن **Google** وفعّله
4. أدخل **Client ID** و **Client Secret** من Google Cloud Console
5. احفظ التغييرات

### 3. إعداد URLs في Supabase

1. في Supabase Dashboard، اذهب إلى **Authentication** > **URL Configuration**
2. تأكد من إضافة:
   - **Site URL**: عنوان تطبيقك (مثل: `https://your-app.lovableproject.com`)
   - **Redirect URLs**: نفس عنوان التطبيق

## ✨ الميزات المفعلة

### ✅ تسجيل الدخول عبر Google
- زر "تسجيل الدخول بواسطة Google" موجود في صفحة تسجيل الدخول
- زر "التسجيل بواسطة Google" موجود في صفحة إنشاء الحساب

### ✅ البيانات الحقيقية والتحديثات الفورية
- Dashboard يعرض بيانات حقيقية من قاعدة البيانات
- تحديثات فورية عند إضافة أو تعديل:
  - المواعيد
  - المرضى
  - الإشعارات
- استخدام Realtime Subscriptions في جميع الصفحات

### ✅ حجز المواعيد المباشر
- نموذج حجز موعد في صفحة تسجيل الدخول
- بحث عن مريض موجود
- إضافة مريض جديد
- حجز موعد مباشرة

### ✅ الإشعارات والتنبيهات
- Service Worker مسجل للإشعارات
- إشعارات فورية عند:
  - إضافة موعد جديد
  - تحديث حالة موعد
  - تسجيل مريض جديد
- عداد الإشعارات غير المقروءة في Sidebar
- صفحة إشعارات كاملة مع تصفية وإدارة

### ✅ المزامنة الحقيقية
- Realtime subscriptions على جميع الجداول:
  - appointments
  - patients
  - notifications
  - medical_records
- تحديث تلقائي للبيانات بدون الحاجة لإعادة تحميل الصفحة
- Toast notifications عند التحديثات

## 🎯 كيفية الاستخدام

### للمستخدمين
1. اذهب إلى صفحة تسجيل الدخول
2. انقر على "تسجيل الدخول بواسطة Google"
3. اختر حساب Google الخاص بك
4. سيتم إنشاء حساب تلقائياً وتسجيل دخولك

### لحجز موعد بدون تسجيل
1. اذهب إلى صفحة تسجيل الدخول
2. اختر تبويب "حجز موعد"
3. ابحث عن المريض أو أدخل بيانات مريض جديد
4. اختر الطبيب والتاريخ والوقت
5. احجز الموعد

## 📱 الإشعارات

لتفعيل إشعارات المتصفح:
1. عند فتح التطبيق لأول مرة، سيطلب منك المتصفح إذن الإشعارات
2. اضغط "السماح"
3. سيتم إرسال إشعارات عند:
   - موعد جديد
   - تحديث حالة موعد
   - مريض جديد

## 🔒 الأمان

- جميع الاتصالات مشفرة عبر HTTPS
- Row Level Security (RLS) مفعلة على جميع الجداول
- OAuth 2.0 لتسجيل الدخول عبر Google
- لا يتم تخزين كلمات المرور في قاعدة البيانات (يتم استخدام Supabase Auth)

## 🚀 الأداء

- استخدام Realtime Subscriptions بدلاً من Polling
- تحديثات فورية بدون إعادة تحميل
- Toast notifications خفيفة وسريعة
- Lazy loading للبيانات

## 📞 الدعم

إذا واجهت أي مشكلة في تفعيل Google OAuth، تواصل مع فريق الدعم أو راجع:
- [Supabase Google Auth Docs](https://supabase.com/docs/guides/auth/social-login/auth-google)
- [Google Cloud Console Help](https://cloud.google.com/docs)
